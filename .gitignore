# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
# Cert requests, keys and certs
*.crs
*.key
*.crt
*.pem
*.srl
*.pub

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
